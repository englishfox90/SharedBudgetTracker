# Database Connection
# For local development with PostgreSQL:
DATABASE_URL="postgresql://postgres:password@localhost:5432/shared_budget_tracker?schema=public"

# NextAuth Configuration
# Generate a secret with: openssl rand -base64 32
AUTH_SECRET="your-secret-here-generate-with-openssl-rand-base64-32"
NEXTAUTH_URL="http://localhost:3000"

# For Railway deployment:
# DATABASE_URL will be automatically set by Railway when you add PostgreSQL service
# NEXTAUTH_URL should be set to your Railway app URL: https://your-app.railway.app
# AUTH_SECRET should be a secure random string (different from development)
